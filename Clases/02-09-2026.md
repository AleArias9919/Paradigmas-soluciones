# Clase 02-09-2026 

## Ejercicio de clase.

### a) Sumar dos números ingresados por teclado y al resultado de esa suma calcularle todos los múltiplos y divisores. 
```smalltalk
| a b c i j top |.

a:= (UIManager default request: 'Enter the firts number') asNumber.
b:= (UIManager default request: 'Enter the second number') asNumber.

c:= (a + b).

Transcript show: 'The result of the addition is: ', c asString; cr.

i:= 1.

[i <= c] whileTrue: [  
	((c \\ i) = 0) ifTrue: [ 
		Transcript show: 'The number ', i asString, ' is a divisor of ', c asString; cr.
		 ].
	i:= i + 1
	]. 

top:= c raisedTo: 3.
j:= (c+c).

[ j <= top ] whileTrue: [ 
	Transcript show: 'The number ', j asString, ' is a multiple of ', c asString; cr.
	j:= j + c
	 ]

```

### b) Idem anterior pero ahora determinar cuántos de esos múltiplos y divisores son pares e impares. 
```smalltalk
| a b c i j top c_par c_imp|.

Transcript cr; cr.

a:= (UIManager default request: 'Enter the firts number') asNumber.
b:= (UIManager default request: 'Enter the second number') asNumber.

c:= (a + b).
i:= 1.
c_par:= 0.
c_imp:= 0.

Transcript show: 'The result of the addition is: ', c asString; cr.
Transcript cr.


Transcript show: '----------------------Divisors----------------------'; cr.
[i <= c] whileTrue: [  
	((c \\ i) = 0) ifTrue: [ 
		Transcript show: 'The number ', i asString, ' is a divisor of ', c asString, ' '.
		((i\\2)=0) ifTrue: [
			c_par:= c_par + 1.
			Transcript show: '(oven-par)'; cr.
			
			]
			ifFalse: [ 
				c_imp:= c_imp + 1.
				Transcript show: '(odd-impar)'; cr ].
		 ].
	i:= i + 1
	]. 
Transcript cr.
Transcript show: 'Even (par) numbers: ', c_par asString; cr.
Transcript show: 'Odd (impar) numbers: ', c_imp asString; cr.
Transcript cr; cr.

top:= c raisedTo: 2.
j:= (c+c).

Transcript show: '----------------------Multiples----------------------'; cr.

[ j <= top ] whileTrue: [ 
	Transcript show: 'The number ', j asString, ' is a multiple of ', c asString; cr.
	j:= j + c
	 ]

```

### Ingresar un número (n) y encontrar todos los números primos menores a n (incluyendo a n) y mostrarlos. 

```smalltalk
Hola, what up?
This is a test
````